# # Use the system clipboard
# set-option -g default-command "reattach-to-user-namespace -l ${SHELL}"

# Enable Shift+Enter to send as-is (pass through to applications)
bind -n S-Enter send-keys Escape "[13;2u"
